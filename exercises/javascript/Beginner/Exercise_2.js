// Simple calculator with a type bug
const a = prompt("Enter first number:");
const b = prompt("Enter second number:");
const sum = a + b;
console.log("Sum is: " + sum); // results in string concatenation
